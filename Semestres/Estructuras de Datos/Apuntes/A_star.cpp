/*
  //Costo mínimo del inicial a un nodo final específico
  
    //HEURÍSTICO
    El costo mínimo se define:
        ~f(n) = g(n) + h(n)     
          *h(n) = aproximación de lo que falta para llegar al destino
          *f(n) = Costo asociado a cada nodo
          *g(n) = Lo que ha costado llegar hasta donde estoy
*/
