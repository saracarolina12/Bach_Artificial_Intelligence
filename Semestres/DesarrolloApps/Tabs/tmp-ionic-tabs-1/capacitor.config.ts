import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'tmp-ionic-tabs-1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
